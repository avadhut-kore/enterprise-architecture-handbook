# Enterprise Error Code Architecture

## 1. Format Standards
Use human-readable, domain-prefixed error codes:
- `ORDER_ALREADY_SHIPPED`
- `AUTH_TOKEN_EXPIRED`
- `PAYMENT_GATEWAY_TIMEOUT`
Avoid cryptic numeric codes (`ERR_98014`).
